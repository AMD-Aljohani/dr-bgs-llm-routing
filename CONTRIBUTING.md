# Contributing

Open an issue before changing scientific inputs, certification rules, scenario definitions, or reported numerical claims. Pull requests should include a description of the change, regenerated outputs when applicable, and a successful run of `python tools/verify_repository.py`.
